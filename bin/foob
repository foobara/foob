#!/usr/bin/env ruby

# TODO: maybe just check for presence of ./Gemfile??
if ENV["LOCAL_FOOB"] == "true"
  ENV["BUNDLE_GEMFILE"] ||= File.expand_path("../Gemfile", __dir__)

  require "bundler/setup"
  require "foobara/foob"
end

foob = Foobara::CommandConnectors::Foob.new

foob.run(ARGV)
